import React from 'react';
import './Accueil.scss';

const Accueil : React.FC = props => {
    return (
        <div className={`accueil`}>
            ACCUEIL
        </div>
    );
}

export default Accueil;