import React from 'react';
import './Photos.scss';

const Photos : React.FC = props => {
    return (
        <div className={`photos`}>
            PHOTOS
        </div>
    );
}

export default Photos;